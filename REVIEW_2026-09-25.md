# Kakeibo 総合レビュー記録（2026-09-25）

この記録はローカルの合成データだけで実施した調査・修正・検証の結果です。作業開始時点で `README.md`、`CHANGELOG.md`、`index.html`、`styles.css`、`js/app.js`、`js/classify.js`、`js/csv.js`、`js/store.js`、`tests/run.mjs` に未コミット変更があり、`js/category-view.js` と `js/category-workbench.js` は未追跡でした。これらを保持して上に修正しました。実データらしき `epos.csv` と既存の公開サイトのデータはテストに使っていません。

## 1. 構成と実装済み機能（Phase 1）

- HTML/CSS/ブラウザー向け JavaScript の静的アプリ。ビルド手順・パッケージ管理・型検査設定なし。グローバル `Store` がメモリ状態を保持する。
- IndexedDB `kakeibo` の `kv` という単一オブジェクトストアに状態全体を JSON として保存。利用できない場合は `localStorage` の `kakeibo.v1` を使用。論理スキーマは v4、IndexedDB の物理バージョンは 1。
- 論理コレクション: `transactions`、`categories`、`accounts`、`budgets`、`recurring`、`recurringSkipped`、`rules`、`merchantAliases`、`categoryChanges`、`templates`、`assetSnapshots`、`importBatches`、`settings`。保存順の識別に `saveSequence` を追加。
- 認証・認可・サーバー API・ネットワーク同期なし。ブラウザープロファイルとオリジン単位でデータ分離。`js/providers.js` の金融機関 API は雛形のみ。外部 AI API は未接続。OCR は同梱 Tesseract.js をブラウザー内で実行。
- 常駐バックグラウンド処理はなし。起動時と月移動時に `applyRecurring`、操作後に 250ms デバウンス保存、OCR は画像選択時のみワーカーを起動。
- テストは `tests/run.mjs`（Node 統合/単体）と今回追加の `tests/perf.mjs`。CI/CD 定義・既知バグ一覧・環境変数設定は見当たらない。既存説明は `README.md` と `CHANGELOG.md`。

| 画面 / 機能 | 実装 | 主なデータ | 今回の確認 |
|---|---|---|---|
| ホーム | 総資産、収支、カテゴリ・日次・月次グラフ、予算、予測 | transactions, accounts, budgets, assetSnapshots | 実画面、合成取引と独立計算 |
| 取引 | 登録・編集・削除・検索・フィルタ・一括操作 | transactions, categories, accounts | 実画面登録、単体/連携テスト |
| 分類整理 | 店舗グループ、未分類キュー、ルール、エイリアス、Undo | transactions, rules, merchantAliases, categoryChanges | 実画面一括分類、統合テスト |
| 分析 | ローカル集計インサイト、自然言語 Query Layer | transactions, budgets, recurring | 画面起動、単体テスト。生成 AI ではない |
| 予算 | カテゴリ予算・使用率・月末予測 | budgets, transactions | 実画面、統合テスト |
| カレンダー | 日別表示・日付からの登録 | transactions | 画面起動。詳細操作は未実施 |
| 資産 | 口座残高、投資評価額・損益、推移 | accounts, transactions, assetSnapshots | 実画面・スマホ表示・計算テスト |
| 固定費 | 定期取引、自動検出、カード引落振替 | recurring, recurringSkipped, transactions | 画面起動、単体テスト |
| データ | CSV入出力、JSON復元、インポート履歴 | transactions, categories, accounts, importBatches | 実画面で取込・再取込、単体/統合テスト |
| 設定 | カテゴリ・口座・ルール・定型管理、全削除 | 全コレクション | 画面起動、口座削除ガードの単体テスト。全削除は実行せず |

HTTP API エンドポイントは 0 件。バックグラウンド処理は上記 3 種。全 10 画面を空の専用オリジンで巡回し、未実装プレースホルダーとブラウザーのコンソールエラーは観測されなかった。各画面のすべての操作を E2E で網羅したわけではない。

## 2–5. バグ、重要度、原因、修正（Phase 2–4、8–9）

| 重要度 | 再現・原因 | 対応と主な変更ファイル |
|---|---|---|
| P1 | CSVプレビューだけで未知のカテゴリ・口座が作られた。プレビュー内で `findOrCreate...` を呼んでいた | プレビューを読み取り専用にし、確定時に解決。`js/csv.js` |
| P1 | プレビューが指紋キャッシュの Set を直接変更し、確定時に全行が重複扱いになり得た | Set をコピーし、確定時に再照合。`js/csv.js` |
| P1 | アプリの CSV に含まれる振替を再取込すると両口座が失われた | 振替元・振替先を列マッピングし、不足時はエラー。`js/csv.js`, `js/views.js` |
| P1 | `2026-02-29` を受理、`1.5` 円を `15` 円と読んだ | 実在日付・整数金額を検証。不正 CSV の引用符も拒否。`js/csv.js` |
| P1 | 投資評価額 120,000 円、投入額 100,000 円でも総資産は投入額で表示。取引のない月の資産推移は現在値を誤表示 | 評価日時点の含み損益を投資残高に反映、以降の純入出金を加算。月末ごとに再計算。`js/store.js`, `js/views.js` |
| P1 | 取引がある口座を削除でき、残高と総資産から消えた | 取引・定期取引・定型・評価額に参照がある口座の削除を拒否。`js/store.js`, `js/views.js` |
| P1 | 同一店舗に食費と日用品の履歴があっても高確信度で片方へ自動分類 | 複数カテゴリ履歴では候補に留める。汎用一括編集による暗黙のルール学習を停止。`js/classify.js`, `js/views.js` |
| P1 | 分類ルール更新後に旧ルールを記録していたため Undo してもルールが戻らない | 更新前のスナップショットを保存。`js/app.js`, `js/category-view.js` |
| P1 | 定期取引の発生分を一括削除すると、次の月移動で再生成された | 一括削除にもスキップ履歴を付け、Undo 時は解除。`js/store.js` |
| P1 | 不正 JSON 復元で文字列金額や存在しない口座を受け入れ得た | 復元前に基本構造・日付・整数金額・口座参照を検証。`js/store.js` |
| P1 | IndexedDB 書込失敗後に localStorage 側の新データを保存しても、次回起動では古い IndexedDB を優先 | 保存順で新しい方を選択し、書込を直列化。読込失敗は起動停止。保存失敗通知と画面離脱時 flush。`js/store.js`, `js/app.js` |
| P1 | 一括カテゴリ編集に収入/振替を混ぜられ、支出カテゴリを付与。振替の口座一括変更は表示だけ成功して残高は不変 | UI とデータ層で種別・口座を検証し、無効な操作を拒否。`js/views.js`, `js/store.js` |
| P2 | スマホ資産画面の 3 列カードで 3 枚目が潰れた | 2 列＋最終カード全幅。実画面 390px で確認。`js/views.js`, `styles.css` |
| P2 | 負残高を `¥-701` と表示、CSV 無効形式や大容量への案内が不足 | `-¥701` に統一、50MB 制限と読込エラー表示。`js/app.js`, `js/views.js` |

P0 に分類できる再現済み事象はありません。上記は修正前のコードと追加した失敗テストで確認。修正後のテストは 43/43 通過しました。

## 6. 機能間の連携（Phase 3）

| シナリオ | 検証結果 |
|---|---|
| A CSV→分類→月次→ホーム | 合成CSV 2件（300/400円）をブラウザーで取り込み、再取込は重複2件スキップ。一括で食費へ変更後、ホーム支出 701円（別のXSSテスト取引1円を含む）、食費700円を確認。Node 統合テストでは合計3,000円、予算使用3,000円、総資産7,000円を確認 |
| B カード購入→引落 | 実画面で収入200,000円、カード購入10,000円、銀行→カードの振替10,000円。総資産190,000円、カード負債0円、月次支出10,000円。再読込後も維持 |
| C 同一店舗→一括→個別→Undo | Node 統合テストで個別修正は維持し、残りだけ Undo。実画面ではグループ2件の一括分類まで確認。全手順の UI E2E は未実施 |
| D 銀行口座間振替 | 単体テストで資産総額・月次収支不変を確認。ブラウザーでは銀行→カード振替で同じ性質を確認 |
| E 取引→予算→月末予測→分析 | 実画面で食費支出10,000円に予算15,000円を設定し、残額5,000円・月末予測12,000円。分析エンジンの数値は単体テスト。全手順の UI E2E は未実施 |

## 7. DB・データ整合性（Phase 4）

金額は JavaScript の安全整数（円単位）へ入力時・CSV・JSON 復元時に制限。振替は単一取引から両口座へ正負を適用し、収支には含めない。取引・残高・月次集計は独立に合成データで照合した。IndexedDB は単一 JSON 値なので DB レベルの FK、ユニーク制約、日付/金額型、セカンダリインデックス、行単位トランザクションはない。コード上の遅延インデックスは月/日/残高/重複指紋。JSON 全体を一度に書くため、永続化は全体置換として原子的だが、異なるタブの同時編集には競合検出がない。

## 8. UI/UX（Phase 5）

デスクトップ全10画面、スマホ幅390pxのホーム・分類・予算・資産を実画面で確認。分類は店舗グループ選択→カテゴリ選択→確認の3操作、通常の取引入力は追加→種別/金額/カテゴリ/口座→保存。スマホの FAB と下部ナビは使える。資産カードの潰れを修正。ダークモード切替を確認。画面リーダーの詳細監査、タッチ端末実機、全フォームのキーボード操作、OCRカメラ操作は未実施。分類の確認操作は誤操作防止として維持した。

## 9. セキュリティ（Phase 6）

サーバー・SQL・ログインがないため SQL Injection、CSRF、API の認可・Rate Limit、ユーザー間データ参照テストは該当しない。金融データはブラウザー内に平文保存され、同じブラウザープロファイル/オリジンを使える人からの隔離はない。入力した `<img ... onerror=...>` を取引一覧に表示しても画像ノードは生成されず、スクリプトも実行されなかった。CSV/JSON 入力は検証を強化。OCR は同梱資産で、AI 外部送信コードなし。CSP、依存資産の改ざん検知、悪意あるバックアップ全体の詳細監査は未実施。

## 10. 性能（Phase 7）

`node --expose-gc tests/perf.mjs`、合成データ、単発の Node プロセス。単位 ms、RSS はプロセス全体の MB。ブラウザー/端末/IndexedDB 実 I/O の応答時間ではない。

| 件数 | 索引 | ホーム集計 | 検索 | 店舗グループ | 12ヶ月資産 | 1,000件一括 | CSVプレビュー | CSV確定 | ピークRSS |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1,000 | 1.8 | 0.2 | 0.7 | 24.5 | 1.1 | 1.6 | 20.6 | 4.0 | 56 |
| 10,000 | 17.3 | 0.1 | 3.2 | 139.5 | 2.7 | 2.8 | 159.3 | 26.3 | 101 |
| 100,000 | 162.4 | 1.2 | 20.0 | 1,338.4 | 39.2 | 7.6 | 1,435.6 | 220.7 | 358 |

店舗グループ化と CSV プレビューの全件処理、単一 JSON 状態とプレビュー配列の多重保持が主な負荷。DB クエリ数/API 応答時間は構成上対象外。ログは `review-perf.log`。

## 11. テスト・再レビュー（Phase 8–10）

- `node tests/run.mjs`: 43/43 通過。ログ `review-test.log`。金額0・負数・小数・安全整数超過・閏日・異常口座・JSON復元・CSV重複・Undo・振替・保存先障害を含む。
- `node --check`（全 JS とテスト）、`git diff --check`: 通過。ビルド・Lint・Type Check は設定がないため未実施（`node --check` は構文検査のみ）。
- ブラウザー E2E: 空の専用 `127.0.0.1` オリジンで画面巡回、カード購入/引落、CSV再取込/分類、予算、再読込永続化、XSS文字列、390px表示を確認。自動 E2E スイートは未導入。
- 未実施: 100,000件の実ブラウザー描画と IndexedDB I/O、通信切断、DB接続障害、タブ同時編集、OCR実機、Safari、レシート画像の大量/異常テスト、完全なアクセシビリティ監査。本番・実データはテストしていない。

## 12. Money Forward ME との公開情報ベースの差分（Phase 11）

2026-09-25 時点で確認できる公式ヘルプ上、Money Forward ME は金融機関との自動連携と、複数端末での家計簿利用、月別予算、資産推移データの出力を提供。家計簿データのダウンロードはプレミアム会員向け。Kakeibo は手入力/CSVとローカル保存、カテゴリ予算、手動の投資評価額、制限のないローカル CSV/JSON 出力を実装している。金融機関の自動同期と複数端末同期は未実装。証券の商品別（株/ETF/投資信託/NISA）内訳も未実装。AI と OCR の方式・精度や料金差は同条件で評価していないため優劣を付けない。公開情報が変わる可能性があるため、追加判断時は公式資料を再確認する。

確認した公式情報: [金融関連サービス連携数](https://support.me.moneyforward.com/hc/ja/articles/12358426689177)、[予算機能](https://support.me.moneyforward.com/hc/ja/articles/900006008226)、[機種変更後の利用](https://support.me.moneyforward.com/hc/ja/articles/900003467106)、[家計簿データのダウンロード](https://support.me.moneyforward.com/hc/ja/articles/49505374073497)。

## 13. 未修正の問題と改善計画（Phase 9–12）

| 優先 | 問題・未対応理由 | 次の実装案 |
|---|---|---|
| P1 | 複数タブの同時更新は最後の保存が勝つ。単一 JSON の現行設計では差分マージがない | 保存時の世代比較・競合検知、他タブ通知、衝突時の安全なバックアップ/再読込フローを先に設計。2タブ E2Eを追加 |
| P1 | 返金/チャージバックが独立した取引種別でない。収入として入力すると支出カテゴリ予算を相殺できない | 元取引に紐付く返金モデル、部分返金、カード残高、予算集計の仕様を決め、移行は非破壊的に実施 |
| P1 | 古い保存データや直接変更された `Store.state` の全フィールドは厳密検証されない。`updateRecurring` も直接パッチ可能 | 永続化境界の検証・不変条件、修復可能な診断画面、非破壊マイグレーションテストを追加 |
| P2 | 資産推移に口座開設日がなく、初期残高を過去月にも適用 | 開始日付きの期首残高を導入し、既存ユーザーには過去の推移が推定であることを表示 |
| P2 | 取引一覧は400件まで、100,000件のグループ化は約1.3秒、CSVプレビューは約1.4秒・ピーク358MB | ページング/仮想リスト、CSV逐次処理、集計キャッシュの細分化。実ブラウザーでメモリと描画時間を再測定 |
| P2 | ストレージ容量不足時は通知するが、ページ終了直前の非同期 IndexedDB 書込完了は保証できない | 保存完了表示と操作直後の永続化、障害時の JSON 救出導線を追加 |
| P2 | 商品別資産、口座自動連携、マルチデバイス同期は未実装 | 費用・保守・プライバシーを見積もり、まず安全な手動商品明細とバックアップ改善を検討。外部連携導入は別途仕様決定 |

本番 DB 削除、破壊的マイグレーション、認証方式変更、有料サービス導入は行っていません。
